testQuery:
  cypher: "MATCH (n) RETURN n"
parameterizedQuery:
  cypher: "MATCH (n:Person) WHERE n.name = '{{name}}' RETURN n"
getAntennaReport:
  cypher: "MATCH (obj:Antenna) {{WHERE_CLAUSE_1}} RETURN obj"
  fieldMapping1:
    manufacturer_type: "obj.manufacturerType"
